---
mode: agent
description: 指定された実装ファイルに対して Vitest の単体テストを作成し、pnpm test が通るまで直す
---

リポジトリの `docs/skills/create-vitest-test.md` を読み、そこに書かれた手順に従って
対象ファイルの単体テストを作成し、`pnpm test` が通るまで修正してください。

手順の正本は `docs/skills/create-vitest-test.md` のみです。このファイルに手順を複製しないでください。

完了したら、作成・変更したテストファイルのパスと `pnpm test` の実行結果を簡潔に報告してください。
