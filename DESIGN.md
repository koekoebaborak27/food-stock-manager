# DESIGN.md — デザインシステム正本

> 「おうちde常備食」の見た目の正本。全 AI ツール（Claude / Codex / Copilot）と人間が共通で従う。
> AGENTS.md / CLAUDE.md からは常時ロードせず、UI 作業時にオンデマンドで参照する。
>
> **色・角丸・間隔の具体値はここに書かない。** 値の正本は `apps/web/src/app/globals.css` の `:root` と `@theme inline`。
> このファイルには「どのトークンをどの用途で使うか」だけを書く（二重管理とずれを避けるため）。
>
> 見た目の参照元は [`docs/specs/02_basic-design/99_デザインイメージ/`](docs/specs/02_basic-design/99_デザインイメージ/README.md)。
> 画面ごとの表示項目・文言は [`docs/specs/02_basic-design/`](docs/specs/02_basic-design/README.md) が正本であり、このファイルは見た目だけを扱う。
>
> 運用ルール: このファイルは「コードと同じ」扱い。3〜6か月ごと、または見た目の破綻が増えたら剪定する。

---

## 1. 原則

- **家庭で毎日使うものとして作る。** 業務システムではない。温かさ・親しみやすさ・生活感を優先し、情報密度を上げすぎない。
- **スマホの縦画面が基準。** 片手で持って、親指の届く範囲で主要な操作が完結すること。PC 表示は破綻しなければよい。
- **開いた瞬間に中身が見えること。** 絞り込みや並び替えの操作を画面上部に積み上げない。食品のカードが最優先で目に入る。
- **意味を色だけで伝えない。** 期限の状態・保存区分・権限の差は、必ず文言かアイコンでも区別できるようにする。
- **一貫性を装飾性より優先する。** 同じ操作・同じ情報は、画面をまたいで同じ部品・同じ見た目で表す。
- **余白とグルーピングで構造を示す。** 罫線を増やさず、余白で情報の階層を語る。
- **トークン外の値を直書きしない。** 新しい値が要るならまず `globals.css` のトークンを増やす。
- **shadcn/ui を土台にし、上書きは Tailwind ユーティリティで最小限に。** 全体の見た目を変えるときは各部品を個別に上書きせず、`globals.css` を編集して一元的に効かせる。

---

## 2. デザイントークン

正本は `apps/web/src/app/globals.css`。**生の HEX を JSX に書かない**（`bg-[#xxxxxx]` は禁止）。

### 2.1 色

**ライトのみ。ダークモードのトークンは持たない。** 端末がダーク設定でもライトで表示する（`layout.tsx` の `colorScheme: "light"`）。暖色の配色は明るい地の上で成立しており、反転させると温かさが壊れるため。

| トークン | 用途 | 見え方 |
| --- | --- | --- |
| `background` / `foreground` | ページ地・本文 | アイボリー地に濃いブラウンの文字 |
| `card` / `card-foreground` | 食品カード・入力パネルの面 | 白 |
| `popover` / `popover-foreground` | ダイアログ・シート・ドロップダウンの面 | 白 |
| `primary` / `primary-foreground` | 主操作（追加ボタン、ログイン、確定） | コーラルオレンジに白文字 |
| `secondary` / `secondary-foreground` | ピル型タグ、補助ボタン、未選択のタブ | 淡いベージュに茶文字 |
| `muted` / `muted-foreground` | 補助テキスト、控えめな背景 | 補助文字は本文より薄い茶 |
| `accent` / `accent-foreground` | ホバー・選択中の面 | ごく淡い暖色 |
| `destructive` / `destructive-foreground` | 削除、期限切れ | 赤に白文字 |
| `border` | カードの縁、仕切り線 | 淡いベージュ。**装飾用** |
| `input` | 入力欄・選択欄の輪郭 | `border` より濃い。**輪郭が操作対象の手がかりになるため別トークンにしている** |
| `ring` | フォーカスリング | コーラル |

**赤（`destructive`）は「期限切れ」と「取り消せない操作」にだけ使う。** これ以外に赤を持ち込むと、危険の合図として働かなくなる。

### 2.2 期限の状態

一覧の中心となる表現なので専用トークンを持つ。**色・文言・アイコンの 3 つで表すこと。** 判定条件と表示する文字列は [`00_共通/00_画面共通.md`](docs/specs/02_basic-design/00_共通/00_画面共通.md) が正本。

| 状態 | 面 / 文字のトークン |
| --- | --- |
| 期限切れ | `expiry-expired` / `expiry-expired-foreground` |
| 今日・明日 | `expiry-today` / `expiry-today-foreground` |
| 3 日以内 | `expiry-soon` / `expiry-soon-foreground` |
| 通常 | `expiry-normal` / `expiry-normal-foreground` |
| 期限なし | タグを出さない |

期限切れだけを塗りつぶしにし、他は淡い面に濃い文字を載せる。期限切れが多い日でも一覧が赤一色にならないよう、**塗りつぶすのはタグだけでカード全体は塗らない。**

### 2.3 タイポグラフィ

| 用途 | クラス |
| --- | --- |
| 画面見出し | `text-2xl font-bold` |
| 食品名・商品名 | `text-lg font-bold` |
| 本文 | `text-base` |
| 補助・日付・作成者 | `text-sm text-muted-foreground` |
| タグの中の文字 | `text-xs` |

- **食品名は太く大きく。** カードの中で最初に目に入るものにする。
- 本文を `text-sm` より小さくしない。タグと補足だけが例外。
- 書体は丸みのある和文ゴシックを優先し、無ければ標準のゴシックへ落とす（`globals.css` の `body`）。Web フォントの読み込みは行っていない。

### 2.4 角丸・間隔・影

- 角丸は `rounded-lg` を既定とする（`--radius` に連動し、テンプレートの既定より大きい）。ボタンとタグは `rounded-full`。
- 間隔は 4px グリッド（Tailwind 標準スケール）に従う。任意値で隙間を作らない。
- **カード間の余白は詰めない。** 一覧では最低でも `gap-3` を空ける。
- 影は使わない。面の区別は背景色と枠線で付ける。例外はシートとダイアログの浮き上がりのみ。

---

## 3. 部品の使い分け

| 用途 | 使うもの |
| --- | --- |
| 食品・買い物リストの 1 件 | カード（白面・大きな角丸・`border`） |
| 保存区分・期限・作り置き | ピル型タグ（`rounded-full`、`secondary` か期限トークン） |
| 保存場所の切り替え | タブ。1 段に収め、常に表示する |
| 新規追加 | 画面右下の丸い追加ボタン（FAB） |
| 削除など取り消せない操作 | 確認ダイアログ |
| 選択肢が少なく、その場で決める操作 | 下から出るシート（残数 0 のときなど） |
| 操作したことの通知・取り消し | 画面下部の短い帯（トースト） |
| 数個から 1 つ選ぶ入力 | ピルを横に並べて 1 タップで選ばせる。ドロップダウンにしない |
| 日付の入力 | カレンダー |

**一覧をテーブルで作らない。** スマホの縦画面に収まらず、残数の増減ボタンも置けない。

**FAB は常備食リストと買い物リストにだけ置く。** 消費済リスト・詳細・登録・設定の画面には置かない。

### 部品の追加

shadcn/ui の部品は、実際に使う画面を作るときに `pnpm dlx shadcn@latest add <部品名>` で追加する（`components.json` は設定済み。実行前にユーザーの確認を取ること）。追加した部品は `apps/web/src/components/ui/` に入る。**`ui/` の中身は原則そのまま使い、見た目を変えたいときは `globals.css` のトークンを直す。**

---

## 4. レイアウト

- 基準は幅 375px の縦画面。まずこの幅で成立させ、広い画面では中央に寄せて最大幅を制限する。
- 一覧画面の上部は **2 段まで**。「保存区分タブ」と「期限が近い食品があるときだけ出る 1 行」以外を常設しない。検索は入力欄を常設せず、アイコンから開く。並び替えはメニューへ収める。
- 下部タブは 3 つ（常備食 / 買い物 / 消費済）。増やさない。常備食リスト・買い物リスト・消費済リストの 3 画面にだけ出し、子画面には出さない（[画面共通](docs/specs/02_basic-design/00_共通/00_画面共通.md)が正本）。
- 主要な操作は画面の下半分に置く。画面上端は情報の表示に使う。
- 下部タブと FAB が内容に重ならないよう、スクロール領域の末尾に余白を確保する。

---

## 5. アクセシビリティ（必須）

- **文字と背景のコントラストは WCAG AA を満たすこと**（本文 4.5:1 以上、枠線など操作の手がかりは 3:1 以上）。トークンはこの基準を満たす値で選んである。**新しい色を足すときは必ず比を計算して確認する。**
- **意味を色だけで伝えない。** 期限の状態には文言とアイコンを必ず添える。保存区分は色を変えず、文字とアイコンで区別する。
- タップできる領域は 44×44px 以上を確保する。残数の増減ボタンとチェックは特に小さくなりやすいので注意する。
- 画像とアイコンだけのボタンには読み上げ用の名前を付ける。
- フォーカスリングを消さない。キーボードだけで一通り操作できること。
- 入力欄には必ずラベルを付ける。プレースホルダだけで用途を示さない。

---

## 6. do / don't

| do | don't |
| --- | --- |
| 食品名を太く大きく出す | 情報を詰め込んで文字を小さくする |
| 期限の状態を色 + 文言 + アイコンで出す | 色だけで期限の危険度を示す |
| 赤は期限切れと削除にだけ使う | 目立たせたい所すべてに赤を使う |
| 選択肢はピルを横に並べる | 3 つ程度の選択肢をドロップダウンにする |
| カードの間に十分な余白を空ける | 一覧をぎっしり詰めて件数を稼ぐ |
| 一覧の上部を 2 段までに抑える | フィルタと並び替えを画面上部に積み上げる |
| トークンで色を指定する | `bg-[#e8724c]` のように値を直書きする |
| ダークは考えず、ライトだけを整える | ダーク用のクラスを個別に足す |

---

## 7. 実装ルール（このリポジトリ固有）

### 配置

- 画面（ルーティング）は `apps/web/src/app/`。
- 機能ごとの部品は `apps/web/src/modules/<機能名>/ui/`。
- shadcn/ui の部品は `apps/web/src/components/ui/`。
- 複数の機能で使う自前の部品は `apps/web/src/components/`。
- クラス名を組み立てるときは `@/lib/utils` の `cn` を使う。

依存の向きは `app → modules → shared` の一方向のみ（詳細は `apps/web/AGENTS.md`）。

### Server / Client の使い分け

- 既定は Server Component。`"use client"` は、状態・イベント・ブラウザ API が要る部品にだけ付ける。
- `"use client"` は葉に近い部品へ付け、画面全体を Client にしない。

### 一覧

- カードの並びで表す。`table` を使わない。
- 読み込み中は、カードの形をした灰色の枠を 3 つ出す。回転する印だけにしない。
- 空のときは、空であることと次にできることを 1 行で示す。

### 通信できないとき

- 操作をさせず、接続が必要である旨を表示する（オフライン対応は行わない方針）。
