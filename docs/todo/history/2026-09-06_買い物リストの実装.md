# 2026-09-06 買い物リストの実装

## 目次

- [2026-09-06 買い物リストの詳細設計を書いた](#2026-09-06-買い物リストの詳細設計を書いた)
- [2026-09-06 ShoppingItemのスキーマと一覧取得APIを実装した（7e-1）](#2026-09-06-shoppingitemのスキーマと一覧取得apiを実装した7e-1)
- [2026-09-06 買い物リスト画面の一覧表示を実装した（7e-2）](#2026-09-06-買い物リスト画面の一覧表示を実装した7e-2)
- [2026-09-06 スタックPRのbase branchが消えて自動クローズされた不具合](#2026-09-06-スタックprのbase-branchが消えて自動クローズされた不具合)

## 2026-09-06 買い物リストの詳細設計を書いた

`docs/specs/03_detail-design/30_買い物リスト/`に2ファイル追加した。

- `01_購入時の常備食反映.md`: 購入時に`ShoppingItem`と`Stock`をまたいで整合させる4分岐の判定条件（sourceStockの有無・削除・消費済・保存区分一致）と、残数上限99の判定・排他制御をコードレベルの手順として書いた。既存の`stock.service.ts`の`adjustQuantity`と同じ「1回の条件付き`updateMany`で判定と更新を同時に行う」方式を踏襲する
- `02_重複判定と一括操作の整合性.md`: 同じ世帯・未購入・未削除の商品名を重複させない制約は、Prismaの`schema.prisma`がWHERE条件付き一意制約を書けないため、マイグレーションSQLへ手で部分一意索引を足す方針にした。一括削除・復元の「全件一致確認」も、存在確認と状態確認を分けて404/409を出し分ける手順にした
- どちらも基本設計とコードだけでは分岐の全体像が追いにくい部分に絞った（`03_detail-design/README.md`の「必要な機能だけ作る」方針。7dでは詳細設計を作らず実装したが、7eの購入時反映ロジックは4分岐＋上限判定＋2テーブルをまたぐ整合性があり、書く価値があると判断した）
- あわせてタスク7eを7d相当の粒度（7e-1〜7e-5）へ分割してTODOへ反映した

## 2026-09-06 ShoppingItemのスキーマと一覧取得APIを実装した（7e-1）

[PR #19](https://github.com/koekoebaborak27/food-stock-manager/pull/19)としてmainへsquashマージ。

- `prisma/schema.prisma`に`ShoppingItem`モデルを追加。`sourceStockId`は`Stock`へのnull許容外部キー（`onDelete: SetNull`）
- 部分一意索引（`householdId`・`name`・`isPurchased = false`・`deletedAt IS NULL`）は、`prisma migrate dev --create-only`で作った雛形のSQLへ`CREATE UNIQUE INDEX ... WHERE ...`を手で追記して実現した
- `apps/api/src/shopping-item/`にservice・controller・moduleを追加し、`GET /api/shopping-items`で未購入→購入済みの順・各々作成日時の古い順に一覧を返す。一覧の`sourceStock`は、未削除の元常備食があれば`{id, storageType, consumedAt}`、削除済み・存在しなければ`null`にする
- 統合テスト（`shopping-item.service.int.test.ts`）で家族グループによる絞り込み・削除済み除外・並び順・`sourceStock`の扱いをローカルDocker Postgresで確認
- `pnpm lint` / `format:check` / `typecheck` / `test`（全体）が通過

## 2026-09-06 買い物リスト画面の一覧表示を実装した（7e-2）

[PR #21](https://github.com/koekoebaborak27/food-stock-manager/pull/21)としてmainへsquashマージ（経緯は次節）。

- `/shopping-list`に`GET /api/shopping-items`を呼ぶ画面（`ShoppingListPage`）を追加。未購入・購入済みに分けて表示し、購入済みは初期状態で閉じ「購入済み（N件）」の見出しで開閉できる（開閉状態は再読込しても保持）
- 商品0件・未購入0件（購入済みのみ）・読み込み失敗の表示を実装
- 商品の追加（7e-3）・購入状態の変更（7e-4）・削除（7e-5）は後続タスクのため、行のチェック・ゴミ箱アイコンとFAB（追加ボタン）は表示のみで`disabled`にした（7d-2でFABの枠だけ先に作った前例と同じ考え方）
- 常備食リスト・消費済リスト画面の下部タブ「買い物」を`/shopping-list`へのリンクに変更
- `pnpm lint` / `format:check` / `typecheck` / `test`（全体）が通過。`dev:web` / `dev:api`を起動し、未ログイン時に`/shopping-list`がログイン画面へリダイレクトされること・コンソールエラーが無いことを確認した。環境にGoogle OAuthの認証情報が無く、ログイン後の実表示はブラウザ未確認（7d-3までと同様の制約）

## 2026-09-06 スタックPRのbase branchが消えて自動クローズされた不具合

7e-1（PR #19）と7e-2（PR #20、PR #19からのスタックPR）は、7b（[積み上がったPRを順にmainへマージした](2026-09-05_アカウント設定の実装.md#2026-09-05-積み上がったprを順にmainへマージした)）と同じ「後続PRが前のPRのブランチから分岐する」パターンで作業していた。

- PR #19を`gh pr merge --squash --delete-branch`でマージした際、そのブランチ（`codex/shopping-list-schema-and-list-api`）を削除した
- PR #20のbaseがそのブランチだったため、GitHubがbase branch消失を検知してPR #20を**自動クローズ**した。`gh pr edit --base main`・`gh pr reopen`はどちらも「baseが存在しないクローズ済みPRは操作できない」という趣旨のエラーで失敗した
- **これは2026-09-05の7bマージ作業（[積み上がったPRを順にmainへマージした](2026-09-05_アカウント設定の実装.md#2026-09-05-積み上がったprを順にmainへマージした)）で一度発生し、「以後はまだ他のPRがbaseにしているブランチは削除しない」と教訓化されていた事象そのもの。今回`gh pr merge --squash --delete-branch`を実行する前にこの教訓を思い出せず、同じ手順を再度踏んでしまった**
- 復旧手順: ローカルの`codex/shopping-list-screen`（PR #20のheadブランチ）はリモートに残っていたため、`git rebase main`でmainの上に載せ替えた（7e-1の内容はすでにsquashでmainに入っているため、同一内容のコミットは自動的にスキップされた）。force-pushの権限が無かったため、書き換えたブランチは`codex/shopping-list-screen-v2`という別名で新規pushし、そこから改めてPR #21（base: main）を作成してマージした。PR #20はクローズ済みのまま経緯をコメントで残した
- **教訓（再掲）**: スタックPRの前段PRをマージするときは`--delete-branch`を付けない。後続PRがまだそのブランチをbaseにしている間は削除せず、後続PRのbaseを`main`へ付け替えてから（または後続PRもマージし終えてから）ブランチを消す。この教訓を次に忘れないよう、プロジェクトメモリ（`project-household-feature-stacked-prs`）にも明記した
