# スキル: <このスキルが何をするか>

<1〜2 行で、何を入力に受け取り、何を成果物として残すか>

このファイルが手順の**正本**。Claude Code / GitHub Copilot / Codex の各入口ファイルは、ここを読ませるだけの薄いラッパーであり、手順を複製しない。

## このスキルの中核（何を担保するか）

- <このスキルが必ず守ること。危険な作業なら「何が危険で、どこで止まるか」を書く>

## 使うタイミング

- 「<こう指示されたとき>」
- <どんな状況で自動起動してよいか>

## 受け取る情報（事前確認）

| 項目 | 必須 | 備考 |
|---|---|---|
| <項目> | ○ | <例や既定値> |

**不明点は推測で埋めない。** コードや既存ドキュメントから判断できることは自分で調べ、判断できないことだけをユーザーに確認する。

## 手順

### 1. <最初にやること>

### 2. <次にやること>

### 3. ユーザーに確認する（必須ゲート。破壊的な操作・ファイル生成を伴う場合）

- 何を・どこに作る（変える）かを提示し、**承認を得るまで実行しない**。

### 4. 検証する

- <どうなれば成功と言えるか。実行するコマンド>

### 5. 報告する

- <何を要約して返すか。試行錯誤の過程は返さない>

## 安全ルール

- `docs/agent_permissions.md` の禁止コマンドを厳守する。
- 秘密情報（接続文字列の実値・パスワード・トークン）を出力・生成ファイル・コミットメッセージに書かない。

## このスキル自体を変更するとき

手順を変えるときは**このファイルだけ**を編集する。各ツールの入口ファイルは触らない。

| 入口 | ファイル |
|---|---|
| Claude Code | `.claude/skills/<name>/SKILL.md` |
| GitHub Copilot | `.github/prompts/<name>.prompt.md` |
| Codex | `.agents/skills/<name>/SKILL.md` |
